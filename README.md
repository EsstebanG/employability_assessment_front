## Frontend

A basic frontend is included using HTML, CSS and JavaScript.

To use it:
1. Open `frontend/index.html` in the browser
2. Register or login
3. You will be redirected to the dashboard
4. List vacancies and apply

The frontend uses `fetch` and Promises to consume the API.
